SELECT
  ins.nome AS instituicao,
  SUM(i.valor) AS volume
FROM
  investimentos i
INNER JOIN produtos p
  ON p.id = i.produto_id
INNER JOIN instituicoes ins
  ON ins.id = p.instituicao_id
GROUP BY
  ins.nome
HAVING
  SUM(i.valor) > 100000
ORDER BY
  volume DESC;
